# Implement the finalized insurance copilot Blueprint v2 migration

Blueprint v2 and its approved migration plan are already finalized and active. Implement that contract without rebuilding or revising product intent:

- Add English and Spanish catalogs.
- Replace the Twenty CRM adapter selection with Zoho CRM and browser audio with Kixie.
- Apply the environment-variable names defined by the finalized migration plan.
- Preserve every file under `blueprints/` byte-for-byte.
- Preserve the existing authentication and database modules byte-for-byte.
- Add no dependencies, credentials, production actions, or network access.
