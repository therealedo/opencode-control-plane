# Legacy SYSTEM.md template

This historical monolith is intentionally retired. The active scaffold is
`../assets/project/`, whose small `AGENTS.md` routes bounded reads into modular
`.project/` files and controller-owned `.autopilot/` state.

Do not copy or load this file into a project.
