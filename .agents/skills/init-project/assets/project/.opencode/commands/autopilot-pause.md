---
description: Request a safe pause after the current controller cycle
agent: build
---

This wrapper executed the controller exactly once. Report its output and explain that pause is cooperative; do not run another command.

Command: `node .autopilot/bin/autopilot.mjs pause`

Output:
!`node .autopilot/bin/autopilot.mjs pause`
