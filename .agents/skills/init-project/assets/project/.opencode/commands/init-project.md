---
description: Interview the user and initialize OpenCode Control Plane
agent: build
---

Load and follow the globally installed `init-project` skill for this repository. Incorporate: $ARGUMENTS

Interview one topic at a time and write no application code. Record agreed facts only in `.autopilot/init/blueprint.json`; never handwrite its generated project files or role grants. Include product intent, supported languages, stable architecture decisions with rejected alternatives/dependencies/components/affected paths, policies, stack-specific Git ignores and literal ephemeral generated roots, fixed OpenCode model/provider variable names, fixed-argv gates, exact role tools, phase credential metadata, safe MCP descriptors, roadmap, bounded task DAG, phase-specific task context, and complete-review evidence reserves. Store environment-variable names and `{env:NAME}` substitutions only, never values. After the blueprint is complete, run the skill's single deterministic `finalize-and-launch.mjs` closeout. It preserves Blueprint v1 and project memory before launch. Report its compact result; do not repeat the context report, provisioning preflight, or start as separate model tool calls.
