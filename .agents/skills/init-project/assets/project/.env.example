# Copy only the variables needed by local development into an ignored env file.
# Test credentials belong in .env.test.local and are injected only into approved gates.
TEST_BASE_URL=https://example.invalid
TEST_USER_EMAIL=test-user@example.invalid
TEST_USER_PASSWORD=
